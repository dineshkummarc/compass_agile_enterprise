<% 
# when using a static form, attributes will be in a hash
# when using a dynamic form, attributes can be sorted and are returned as an array
# this may possibly be simplified under Ruby 1.9 with ordered hashes
attrs = @website_inquiry.data.sorted_dynamic_attributes(false)
if attrs.is_a?(Hash) 
  @website_inquiry.data.sorted_dynamic_attributes(false).each do |k,v| %>
<%=k %>: <%=v %>
<% end 
else  
  @website_inquiry.data.sorted_dynamic_attributes(false).each do |field|
field.each do |k,v| %>
<%=k %>: <%=v %>
<% end
 end 
end %>
