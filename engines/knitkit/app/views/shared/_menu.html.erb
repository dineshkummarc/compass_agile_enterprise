<% unless @sections.nil? %>
  <ul>
    <% @sections.each do |section| %>
      <li>
        <a href="/<%= section.permalink %>"><%= section.title %></a>
      </li>
    <% end %>
  </ul>
<% end %>