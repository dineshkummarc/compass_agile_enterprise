Given /^a table with (\d+) row with an id column$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^I update the row with some new data$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the row with a matching id should be updated in the selected database$/ do
  pending # express the regexp above with the code you wish you had
end

