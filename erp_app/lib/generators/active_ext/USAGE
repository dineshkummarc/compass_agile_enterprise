Description:
  Used to create Active Ext scaffold for a model 
  the Compass AE framework

Example:
    rails g active_ext [model_name], [container (desktop | organizer)], [application_name]

    This will create:
       1. controller in application's folder
       
       2. javascript file in application's javascripts folder
       
       3. append to routes file



