<h2><%=h @website_section.title %></h2>

<% @contents.each do |content| %>
  <%=raw content.body_html %>
<% end %>

<%= render_widget :login, 
                  :params => {:logout_to => "/login", 
                              :login_to => "/home", 
                              :signup_url => '/sign-up'} %>
